---
description: Run an adaptive study session for the 5-Day AI Agents course
---

Read `tutor/TUTOR.md` and follow its protocol exactly to run a study
session now.

- Session type: `$ARGUMENTS` if given (session / quick / exam), otherwise
  default to `session`.
- Get today's date by running `date +%F` with the Bash tool — do not ask
  the user for it.
- Read `tutor/progress.md` for state and update it yourself at the end of
  the session.
- Read only the lesson files you need (`tutor/lessons/day<N>.md` for the
  current day and for any due review cards).
